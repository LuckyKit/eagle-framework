interface:
  display_name: "eagle dev — 功能开发（编排）"
  short_description: "编排 Skill。协调 discuss → plan → coder → tester → reviewer → knowledge-writer 完整流程。"
  default_prompt: "Use $eagle-dev to run the matching Eagle workflow."
